# DEPRECATED: This workflow has been merged into the main CI pipeline
# Use ci.yml or .github/workflows/reusable/reusable-circuits.yml instead
# This file is kept for backwards compatibility but will be removed in a future update

name: circomspect (DEPRECATED)
on:
  pull_request:
    paths:
      - 'circuits/**'
  push:
    branches: [main]
    paths:
      - 'circuits/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read
  security-events: write

jobs:
  deprecated-notice:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow is deprecated. Circomspect analysis is now integrated into the main CI pipeline (ci.yml)."
          echo "::warning::Please update your branch protection rules and references to use ci.yml instead."
