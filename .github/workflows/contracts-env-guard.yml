# DEPRECATED: This workflow has been consolidated into security.yml
# Use .github/workflows/security.yml instead
# This file is kept for backwards compatibility but will be removed in a future update

name: Contracts Env Guard (DEPRECATED)

on:
  pull_request:
    paths:
      - 'contracts/**'
      - '.github/workflows/contracts-env-guard.yml'
  push:
    branches:
      - main
      - dev
    paths:
      - 'contracts/**'
      - '.github/workflows/contracts-env-guard.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  deprecated-notice:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow is deprecated. Contracts environment guard is now part of the unified security.yml workflow."
          echo "::warning::Please update your branch protection rules and references to use security.yml instead."
