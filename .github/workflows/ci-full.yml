# DEPRECATED: This workflow has been replaced by the unified ci.yml
# Use ci.yml with mode: full instead
# This file is kept for backwards compatibility but will be removed in a future update

name: CI Full (DEPRECATED)
on:
  workflow_dispatch:
  schedule:
    - cron: '30 2 * * *' # Daily at 2:30am UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read
  security-events: write

jobs:
  deprecated-notice:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow is deprecated. CI Full is now integrated into the unified CI pipeline (ci.yml)."
          echo "::warning::Please update your branch protection rules and references to use ci.yml with mode: full instead."
