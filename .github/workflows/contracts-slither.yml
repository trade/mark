name: Contracts Slither

on:
  pull_request:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  slither-core:
    uses: ./.github/workflows/_reusable-contracts-slither.yml
