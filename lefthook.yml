pre-commit:
  parallel: true
  commands:
    typecheck:
      run: pnpm -s typecheck
    lint:
      run: pnpm -s lint

pre-push:
  commands:
    contracts-fast:
      run: cd contracts && make ci-fast
